import 'package:flutter/material.dart';

class BranchDevelop extends StatelessWidget {
  const BranchDevelop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
